---
title: "VHL snRNAseq report"
output: html_document
---

``` {r setup}
knitr::opts_chunk$set(echo = FALSE, 
                      message = FALSE, 
                      warning = FALSE, 
                      cache = FALSE)
# pass params to the global environment
list2env(params, globalenv())
``` 



``` {r get_data}
    library(thepackage)
    dataset <- get_data(data)
    dataset_ready <- thepackage:::impute_values(data, impute_func)
``` 

``` {r summary}
    summary(dataset_ready)
``` 
